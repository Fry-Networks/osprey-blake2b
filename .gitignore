# GHDL work library
src/ghdl-work/
work-obj*.cf
*.o

# Python
__pycache__/
*.pyc
*.pyo

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Vivado + Xilinx
*.bit
*.bin
*.dcp
*.jou
*.log
*.rpt
.Xil/
project_*/
vivado*.backup.*
webtalk*.jou
webtalk*.log
.hbs
usage_statistics_webtalk.*

# Operator rollback state (NOT part of source)
backups/

# Vendored second-party clone that carried its own .git
vendor/sia-fpga/.git/
vendor/knots/
vendor/pachira-algo/

# Raw HARD GATE per-vector output (summary is committed instead)
reports/hardgate-*-raw.txt

# Cross-compiled miner binaries. The ARM build is shipped from the deploy repo
# (Fry-Networks/osprey-algo-blake2b), where the device clones it; a second copy
# here would just drift.
miner/blake2b
miner/blake2b_host

# Vivado per-iteration congestion scratch
iter_*_CongestedCLBsAndNets.txt

# GHDL simulation output
stage4_pairs.txt
stage4_compare_pairs.txt

# Publish-audit output. It is a run artifact, not source, and its key names have
# embedded the operator's handle before now.
reports/publish-audit.json

# Local rollback copies (.bak.<timestamp>). These are this machine's undo path,
# not source; the bitstream ones are also ~21MB each.
*.bak
*.bak.*
miner/siacoin
miner/siacoin_host
sia_pairs.txt
src/sia/ghdl-work/
